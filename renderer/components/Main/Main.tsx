import Header from './Header';
import Tasks from './Tasks';

const Main: React.FC = () => {
    return (
        <main>
            <Header />
            <Tasks />
        </main>
    );
};

export default Main;
